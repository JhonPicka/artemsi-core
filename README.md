# ARTEMSI — application Next.js

Code source : `src/` (App Router, API routes, composants, `lib/`).

## Demarrage

Depuis ce dossier :

```bash
npm install
cp .env.example .env.local   # puis remplir les variables
npm run dev
```

Documentation produit, migrations Supabase, offres/matching, Stripe : voir le
[**README a la racine du depot**](../README.md).
